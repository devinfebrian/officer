# Documentation

- Wants a Product Requirements Document (PRD) drafted as part of the design process, in addition to the technical design spec — product docs capture the "what/why" (problem, personas, metrics, scope) alongside the architecture "how". Explicitly requested: "i think we should design the system and draft Product Requirements Document". Confidence: 0.8

- Prefers product-vision framing even for a hackathon/demo project: when offered "hackathon deliverable" vs. "real product vision" vs. "both", chose the real-product framing (personas like Operations Lead / Internal Auditor, product success metrics, non-goals, risk table) rather than demo-only documentation. Confidence: 0.7

- Wants a written implementation plan as a distinct deliverable before any code is written, following the spec/PRD stage of the design workflow (explicitly asked "create implementation plan" after the spec and PRD were approved; plan written in plan mode with milestones, project layout, and verification). Confidence: 0.8

- Wants implementation plans structured as standalone per-feature session cards so each feature can be picked up and executed in a fresh session independently — each card self-contained with dependencies, exact files to create, concrete steps, done-when criteria, and a verify command, plus shared context (stack/layout/decisions) up top and build order noted. Explicitly requested: "create implementation plan only so i can work on each features individually on a new session". Confidence: 0.9
